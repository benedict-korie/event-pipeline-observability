services:
  service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080

  prometheus:
    image: prom/prometheus:v2.53.0
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - service
